# Epic 3 Retrospective — lets-do-it

**Epic:** Mark progress and ship the full core loop with quality gates  
**Date:** 2026-05-13  
**Sprint status:** All four stories (3.1–3.4) marked **done**; this document closes the epic retrospective.  
**Facilitation style:** Systems and process focus; no blame.  
**Sources:** Story files `3-1` … `3-4`, `epic-2-retro-2026-05-12.md`, `deferred-work.md`, `epics.md`, PRD, architecture.

---

## Epic Summary

Epic 3 completed the V1 core loop: users can toggle todo completion through a done-only PATCH contract, see accessible done styling, rerun the SC-01 add → complete → delete smoke path, and rely on CI to run unit, E2E, and full-flow axe checks.

**Stories completed:** 3.1, 3.2, 3.3, 3.4 (four of four).

---

## What Went Well

- **Faster development.** Gauravsingh identified faster dev as the standout win. The epic moved quickly because stories were tightly scoped, context-rich, and review patches were handled immediately instead of becoming loose follow-up work.
- **Brownfield discipline stayed strong.** Story 3.1 hardened an already-present PATCH implementation through traceable tests/docs rather than rewriting it. Story 3.2 made one production code change and focused on proving behavior.
- **Quality gates became real, not aspirational.** Story 3.3 added the SC-01 E2E smoke path, and Story 3.4 extended axe coverage to add → complete → delete in CI.
- **Epic 2 retro follow-through landed.** B1 and B2 were finally closed in Story 3.4: CI-safe Playwright global setup, explicit API test isolation, and `workers: 1` rationale.

---

## Challenges and System Improvements

No major execution friction was reported by Gauravsingh during the retrospective.

The remaining items are process/tooling cleanup rather than feature blockers:

- **B3 remains open:** `last_updated` in `sprint-status.yaml` is still a manual field and has repeatedly created review noise.
- **Playwright config clarity:** `fullyParallel: true` with `workers: 1` behaves serially today, but the combination is easy to misread.
- **Axe retry policy:** CI retries can hide a flaky axe-gate failure if the first run fails and retry passes.
- **Deferred technical debt still exists:** older API/client hardening items remain in `deferred-work.md`.

---

## Epic 2 Retro Follow-Through

| # | Action | Status | Evidence |
|---|--------|--------|----------|
| B1 | Add CI guard to Playwright `globalSetup` | ✅ Completed | Story 3.4 added `if (process.env.CI) return;` before Compose startup |
| B2 | Add Vitest integration isolation and Playwright worker rationale | ✅ Completed | Story 3.4 added `pool: "forks"` + `singleFork: true`; Playwright now has `workers: 1` with rationale |
| B3 | Automate or remove `last_updated` | ❌ Still open | Sprint status continues to require manual timestamp updates |

---

## Patterns Identified Across Stories

| Pattern | Impact |
|---------|--------|
| Context-rich story files | Reduced ambiguity and sped up implementation |
| Code review as tightening loop | Small, targeted patch sets improved reliability without broad churn |
| Row-scoped E2E selectors | Reduced false-pass risk in toggle and core-loop tests |
| Deferred-work continuity | B1/B2 were carried forward until the correct owning story |
| Manual status metadata | `last_updated` remains fragile and should be removed or automated |

---

## Technical Debt and Follow-Ups

See `deferred-work.md` for the canonical backlog. Items most relevant after Epic 3:

| Theme | Suggested owner |
|-------|-----------------|
| Automate/remove `last_updated` | Next tooling cleanup |
| Clarify Playwright `fullyParallel` vs `workers: 1` | E2E infrastructure cleanup |
| Decide axe retry policy | QA / CI cleanup |
| Axe-core version alignment across unit/E2E layers | Accessibility tooling cleanup |
| Older API/client hardening items | Future maintenance pass |

---

## Next Epic Preview

No Epic 4 is currently defined in `epics.md`.

That means Epic 3 closes the currently planned MVP arc. The next move should be either:

1. Define the next product direction and create new epics/stories, or
2. Run a cleanup pass on deferred technical/process items before planning more feature work.

No significant discovery from Epic 3 invalidates the existing V1 plan. Epic 3 completed the planned core loop and quality-gate scope.

---

## Readiness Assessment

| Area | Status |
|------|--------|
| Story completion | 4/4 ✅ |
| Core loop | Add → complete → delete covered by Playwright ✅ |
| Accessibility gate | Full-flow axe gate with zero critical violations ✅ |
| CI documentation | README and CI workflow aligned ✅ |
| Previous retro B1/B2 | Completed ✅ |
| Previous retro B3 | Still open ⚠️ |
| Next epic | Not defined ⚠️ |

Epic 3 is complete from a story and quality-gate perspective.

---

## Action Items

### Process / Tooling

| # | Action | Owner | Success Criteria |
|---|--------|-------|------------------|
| C1 | Automate or remove `last_updated` in `sprint-status.yaml` | Dev / tooling | No future code review finding for timestamp regression |
| C2 | Clarify Playwright parallelism policy (`fullyParallel` + `workers: 1`) | Dev / QA | Config intent is obvious and cannot be misread as parallel-safe DB isolation |
| C3 | Decide axe retry policy for CI gate | QA / CI | Team chooses either no retries for axe specs or retry-rate tracking |

### Planning

| # | Action | Owner | Success Criteria |
|---|--------|-------|------------------|
| C4 | Choose next direction: new product epic vs cleanup pass | Product Lead | Clear next BMad workflow selected |
| C5 | If continuing product work, create Epic 4 planning artifacts | Product / Architecture | New epics/stories exist before `bmad-create-story` resumes |

---

## Team Voice

Amelia (Developer): Epic 3 moved fast because the work stayed focused and review feedback turned into immediate patches.

Alice (Product Owner): The MVP core loop is now demonstrable and guarded by CI-quality checks.

Charlie (Senior Dev): B1 and B2 getting closed in Story 3.4 removed the biggest infrastructure uncertainty carried from the previous retro.

Dana (QA Engineer): The SC-01 smoke and full-flow axe gate are the main quality wins; the next cleanup should reduce process noise around status metadata and CI policy.

Elena (Junior Dev): The story context and brownfield audit pattern made the work easier to follow and less error-prone.

Gauravsingh (Project Lead): Faster dev was the biggest win; no major friction reported.

---

## Next Steps

1. ✅ Mark `epic-3-retrospective` done in `sprint-status.yaml`.
2. Commit the retrospective artifact and sprint-status update.
3. Choose between a cleanup pass or new product planning.

---

_Retrospective generated as part of BMad `bmad-retrospective` workflow — Epic 3 complete._
